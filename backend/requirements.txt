# Veritas backend — Python 3.10+
# Install: pip install -r requirements.txt

# ── Web framework ────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
httpx>=0.27

# ── Configuration ─────────────────────────────────────────────────
python-dotenv>=1.0

# ── Cloud database (Supabase Postgres) ───────────────────────────
supabase>=2.0

# ── Audio ingestion ──────────────────────────────────────────────
yt-dlp>=2024.3
certifi

# ── PDF generation ───────────────────────────────────────────────
fpdf2>=2.7

# ── Audio processing & feature extraction (Feature 2.1) ─────────
librosa>=0.10
soundfile>=0.12
numpy>=1.26
scipy>=1.12

# ── Deep learning inference (Feature 2.2 primary model) ─────────
# CPU-only torch is sufficient; GPU will be used automatically if available.
torch>=2.0
transformers>=4.38
accelerate>=0.26
